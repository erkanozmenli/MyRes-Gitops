apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq-svc

spec:
  template:
    spec:
      containers:
      - name: rabbitmq
        env:
          - name: RABBITMQ_DEFAULT_USER
            value: guest
          - name: RABBITMQ_DEFAULT_PASS
            value: guest